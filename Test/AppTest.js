import {View, Text, SafeAreaView} from 'react-native';
import React from 'react';

const AppTest = () => {
  return (
    <SafeAreaView>
      <Text>AppTest</Text>
    </SafeAreaView>
  );
};

export default AppTest;
