import { View, Text } from 'react-native';
import React from 'react';
import ActivityIndicatorTest from './1_ActivityIndicator';
import App from './2_Button';
import FlatList from './3_FlatList';
import ImageBackgroundTest from './5_ImageBackground';
import ModalTest from './6_Modal';
import Pressable from './7_Pressable';

// The <Button /> component accepts title and onPress props but it does not accept a style prop, which makes it hard to customize the style.

const AppLearnCoreComponents = () => {
    return <Pressable />;
};

export default AppLearnCoreComponents;
