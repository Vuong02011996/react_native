/**
 * 1. Setup redux store
 * - Reducers & root reducer(index.js)
 * - Action creator
 * - Store
 * 2. Setup redux provider
 * - Allow redux store to be accessible from anywhere of the app
 * 
 * 3. Connect to redux store from component.
 * - Use hooks useSelector, useDispatch 
 */

 